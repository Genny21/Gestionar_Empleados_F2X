serenity {
  logging = VERBOSE
  browser.width = 1400
  browser.height = 800
  browser.maximized = true
  timeouts.implicitlywait=150
  take.screenshots = AFTER_EACH_STEP
}


timeouts {
    implicitlywait = 150
    fluentwait = 150
}

        feature.file.encoding = iso-8859-1
        enable.markdown=story narrative scenario step
        serenity.include.actor.name.in.consequences = true

  # codificacion en la generacion del reporte
    report.encoding = utf8

    # Nombre del proyecto de automatizacion que saldra en el reporte
    project.name = "Automatización de Pruebas API - ReqRes"


environments {
                default {

                path.users = "/users"
                path.user = "/users/"
                path.register = "/register"
                    }

                dev {

               path.users = "/users"
               path.user = "/users/"
               path.register = "/register"

                                    }
                stg {

                path.users = "/users"
                path.user = "/users/"
                path.register = "/register"

                    }
                pro {

               path.users = "/users"
               path.user = "/users/"
               path.register = "/register"

                    }
}
